/*
  Mime Types
  1. create mysite/style.css
    -> though in Network tab -> we see style.css -> but css does not show -> it is because in Headers -> content-type: text/plain

  🌲 nginx comes with default mime types -> conf/mime.types
    -> we can include this file as our mime types



 */
